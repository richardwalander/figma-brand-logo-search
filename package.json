{
  "name": "figma-brand-logo-search",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "watch": "vite build --watch && vite build --watch --config vite.lib.config.js ",
    "build": "vite build && vite build --config vite.lib.config.js",
    "preview": "vite preview"
  },
  "dependencies": {
    "preact": "^10.23.1",
    "react-jss": "^10.10.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.0",
    "concurrently": "^8.2.2",
    "esbuild": "^0.23.1",
    "vite": "^5.4.2",
    "vite-plugin-singlefile": "^2.0.2"
  }
}
